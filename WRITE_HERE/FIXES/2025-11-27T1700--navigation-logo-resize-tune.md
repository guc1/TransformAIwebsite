# Navigation logo scale tuned per page
- **What:** Detected the home route to keep the hero-sized logo on load, started other pages with the compact logo, and reduced the settled scale so the mark shrinks faster to a slightly smaller final size.
- **Why:** The user asked for the scrolled logo to end up a bit smaller overall and to have subpages start at the compact size instead of the hero-scale treatment reserved for the homepage.
- **Files:** `apps/www/components/navbar/navigation.tsx`.
- **Follow-ups:** Capture updated navigation screenshots after the environment can run with `next-intl` installed.
